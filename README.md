# 🎮 坦克大戰 Tank Battle

<div align="center">

![React](https://img.shields.io/badge/React-19-61DAFB?logo=react&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-7-646CFF?logo=vite&logoColor=white)
![HTML5 Canvas](https://img.shields.io/badge/HTML5-Canvas-E34F26?logo=html5&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green)

**一款支援 RWD 響應式佈局與觸控操作的經典坦克大戰網頁遊戲**

*Created by Poyu Chen © 2026*

[English](./README.en.md) | 繁體中文

</div>

---

## 📖 簡介

使用 React + HTML5 Canvas 打造的經典坦克大戰遊戲，完整支援桌機鍵盤操作與行動裝置觸控操作。遊戲畫布會根據螢幕大小自動縮放，在手機、平板與桌機上皆能流暢遊玩。

## ✨ 功能特色

- 🖥️ **響應式畫布** — Canvas 自動依視窗大小縮放，保持 16:9 比例
- 📱 **手機優先設計** — 極薄 HUD、內嵌工具按鈕，最大化遊戲畫面與控制區
- 👴 **老人友善操作** — 大型觸控按鈕（44×44px+），手機上控制區填滿剩餘空間
- 🎮 **雙模式控制** — 桌機用鍵盤/滑鼠、手機用觸控，虛擬按鈕永遠可見
- 🎵 **8-bit 音效系統** — Web Audio API 生成復古音效（發射、爆炸、通關），背景音樂可切換
- 🤖 **敵軍 AI** — 隨機巡邏移動與自動開火
- 🧱 **地圖系統** — 可破壞磚牆 + 不可破壞鐵牆，隨關卡自動生成
- 💥 **碰撞偵測** — 坦克 vs 牆壁、坦克 vs 坦克、子彈 vs 坦克
- 🏆 **關卡系統** — 每關擊敗 8 台敵軍即可過關，難度遞增
- ⛶ **全螢幕模式** — 一鍵切換全螢幕，沉浸式體驗
- 🔄 **經典網格對齊** — 轉向自動 Snap-to-Grid，操作手感流暢

## 🎯 操作方式

| 操作 | 鍵盤 | 觸控 / 滑鼠 |
|------|------|-------------|
| 移動 | `W` `A` `S` `D` 或 方向鍵 | 虛擬方向鍵 (D-Pad) |
| 射擊 | `空白鍵` 或 `J` | FIRE 按鈕（紅色圓形） |
| 暫停 | `P` 或 `Esc` | 暫停按鈕 ⏸（手機在控制區中間） |
| 全螢幕 | `F` | ⛶ 按鈕（手機在控制區中間） |
| 音效切換 | — | 🎵 按鈕（畫面右上角） |

**手機直向模式**：HUD 極薄（28px）、工具按鈕內嵌在控制區、Canvas 全寬顯示、剩餘空間全給大型觸控按鈕。

## 🚀 快速開始

```bash
# 複製專案
git clone https://github.com/chenpoyu/games-tank-battle.git
cd games-tank-battle

# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev

# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

## 🏗️ 專案結構

```
games-tank-battle/
├── index.html                    # 進入點 HTML
├── vite.config.js                # Vite 設定（含 GitHub Pages base path）
├── package.json
├── .github/
│   └── workflows/
│       └── deploy.yml            # GitHub Actions 自動部署
├── src/
│   ├── main.jsx                  # React 進入點
│   ├── App.jsx                   # 根元件
│   ├── components/
│   │   ├── TankGame.jsx          # 主遊戲元件（Canvas + 遊戲迴圈）
│   │   ├── VirtualControls.jsx   # 虛擬觸控按鈕
│   │   ├── GameHUD.jsx           # 狀態顯示列（分數/生命/關卡）
│   │   └── BackgroundMusic.jsx   # 背景音樂播放器
│   ├── hooks/
│   │   ├── useResponsiveCanvas.js # 響應式 Canvas 尺寸計算
│   │   └── useInputHandler.js     # 鍵盤 + 觸控輸入統一處理
│   ├── game/
│   │   ├── constants.js          # 遊戲常數與設計解析度
│   │   ├── GameEngine.js         # 核心遊戲引擎（邏輯層）
│   │   ├── Tank.js               # 坦克類別（含網格對齊）
│   │   ├── Bullet.js             # 子彈類別
│   │   ├── Wall.js               # 牆壁類別（磚牆/鐵牆）
│   │   ├── mapGenerator.js       # 地圖生成器
│   │   └── SoundEffects.js       # 音效管理器（Web Audio API）
│   └── styles/
│       └── GameUI.css            # RWD 響應式樣式
```

## 🛠️ 技術細節

| 技術 | 用途 |
|------|------|
| React 19 | UI 元件與狀態管理 |
| HTML5 Canvas | 遊戲繪製引擎 |
| Web Audio API | 8-bit 風格音效與背景音樂生成 |
| Vite 7 | 建置工具與開發伺服器 |
| CSS Flexbox/Grid | 響應式佈局 |
| Mobile-First RWD | 手機優先斷點設計（≤480px / 481-768px / >768px） |
| Touch Events API | 行動裝置操作 |
| requestAnimationFrame | 60fps 遊戲迴圈 |
| GitHub Actions | CI/CD 自動部署至 GitHub Pages |

## 📦 部署

本專案已設定 GitHub Actions，推送至 `main` 分支即自動部署至 GitHub Pages：

1. 在 GitHub Repository 的 **Settings → Pages** 中，將 Source 設為 **GitHub Actions**
2. 推送程式碼至 `main` 分支
3. Actions 將自動建置並部署，完成後即可透過 `https://chenpoyu.github.io/games-tank-battle/` 訪問

## 📄 授權

MIT License

Copyright © 2026 Poyu Chen. All rights reserved.

## 👨‍💻 作者

**Poyu Chen**
- GitHub: [@chenpoyu](https://github.com/chenpoyu)
- Project: [games-tank-battle](https://github.com/chenpoyu/games-tank-battle)
